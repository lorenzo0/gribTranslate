/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package LetturaGrib;
import com.ph.grib2tools.grib2file.GribFile;
import java.util.*;
import java.io.*;

import java.io.File;
import java.nio.charset.StandardCharsets;
//import GRIB2Tools-master.*;

/**
 *
 * @author loren
 */
public final class Grib2Input extends java.lang.Object{
    /*
    Grib2Input grib2Input;
    String pathfile = "format.grib";
    
    GribFile input;
    
    InputStream is;
    
    String fileName = "src/resources/thermopylae.txt";

        try (InputStream fis = new FileInputStream(fileName);
                InputStreamReader isr = new InputStreamReader(fis,
                        StandardCharsets.UTF_8);
                BufferedReader br = new BufferedReader(isr)) {

            br.lines().forEach(line -> System.out.println(line));
        }
    

    //Create RandomAccessFile
    RandomAccessFile gribfile = null;
    
    try {
      gribfile = new ucar.unidata.io.RandomAccessFile(pathfile, "rw");
    } catch (IOException e) {
      e.printStackTrace();
    }

    //Create grib2Input file
    Grib2Input file = new Grib2Input();      
    try {
      grib2Input.scan(false,  false);       
    } catch (IOException e) {
      e.printStackTrace();
    }
*/
}
